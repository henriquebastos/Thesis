# while walking through executed code:
   possible_class_name = result.split('=')[1]
   if 'instance' in possible_class_name:
      possible_class_name = possible_class_name.split(' instance')[0]
      possible_class_name = possible_class_name.rstrip('.')
      if possible_class_name in data['classes']
         # Create new Generic Object
         # if class has __init__ function
            # store latest_generic object
            # store latest lines
   if current_lineno in latest_lines:
      if 'result' in current:
         var = .split('=')[0]
         # add var to class with var = to result string
   else:
      latest_lines = None


   # Handle function calls to class

   # Handle assigned lines
